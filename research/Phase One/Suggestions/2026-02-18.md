---
date: 2026-02-18
generated_by: claude
---

# Next Task Suggestions — 2026-02-18

## Follow-up from Previous Suggestions

> Status of items suggested in previous runs.

| Date       | Suggestion                             | Status                     |
| ---------- | -------------------------------------- | -------------------------- |
| 2026-02-09 | Resolve Pending Architecture Decisions | Actioned — Done 2026-02-09 |
| 2026-02-09 | Database Setup & Schema                | Actioned — Done 2026-02-13 |
| 2026-02-09 | Backend Authentication & Authorization | Actioned — Done 2026-02-17 |
| 2026-02-09 | Shared Types Package                   | Actioned — Done 2026-02-18 |
| 2026-02-09 | System Metric Collection Service       | Actioned — Done 2026-02-18 |
| 2026-02-09 | Dashboard API Endpoints                | Pending                    |
| 2026-02-09 | Monitoring API Endpoints               | Pending                    |
| 2026-02-09 | Logs & Notifications API Endpoints     | Pending                    |
| 2026-02-09 | Users Management API                   | Pending                    |
| 2026-02-09 | Frontend Auth Integration & Routing    | Pending                    |
| 2026-02-17 | Dashboard API & WebSocket              | Pending                    |
| 2026-02-17 | Monitoring API Endpoints               | Pending                    |
| 2026-02-17 | Logs & Notifications API               | Pending                    |
| 2026-02-17 | Users Management API                   | Pending                    |
| 2026-02-17 | Frontend API Integration               | Pending                    |

> 5 of 10 original suggestions actioned. System Metric Collection Service completed today — all foundation and data-pipeline work is now done. The entire remaining work is backend API development and frontend integration.

---

## Current Project Snapshot

- **Tasks Done**: 7
- **Tasks In Progress**: 0
- **Tasks To Do**: 0
- **Subtasks Done**: 23
- **Subtasks In Progress**: 0
- **Subtasks To Do**: 0
- **Open Blockers**: 0
- **Pending Decisions**: 0

> All foundation work is complete: UI, architecture decisions, database, auth, shared types, and system metric collection. The project has **zero active tasks** — all remaining work (backend APIs + frontend integration) must be created as new tasks.

---

## Resolve First

> No blocking decisions or open blockers.

_Nothing to resolve — proceed with task creation._

---

## Suggested Tasks

> New main tasks recommended for creation, ordered by dependency.

### 1. Dashboard API & WebSocket

- **Priority**: High
- **Rationale**: With metric collection now running, the Dashboard API is the next critical milestone — it's the first page to display real persisted data end-to-end. Needs REST endpoints for initial page load (summary, historical charts, storage mounts) and a WebSocket server for live 15-second metric pushes per the WebSocket decision.
- **Depends on**: System Metric Collection Service (Done ✓)
- **Scope**: Implement `GET /api/dashboard/summary`, `GET /api/dashboard/metrics`, and `GET /api/storage/mounts` per `api_specification.md`. Set up WebSocket server integrated with Express. Broadcast new metric batches to authenticated connected clients every 15 seconds.
- **Suggested subtasks**:
  - Implement `GET /api/dashboard/summary` (latest CPU, memory, disk, network values + uptime)
  - Implement `GET /api/dashboard/metrics` (historical time-series from SystemMetric table, filtered by type + period)
  - Implement `GET /api/storage/mounts` (read mount info via `/proc/mounts` or `df`, return per-mount stats)
  - Set up WebSocket server (ws or socket.io) integrated with Express app
  - Implement WebSocket authentication (validate JWT cookie on connection handshake)
  - Broadcast metric batch to connected clients after each collector cycle

### 2. Monitoring API Endpoints (Processes, Services, Docker, Node.js)

- **Priority**: High
- **Rationale**: These power the four main monitoring pages. All are real-time system queries — no dependency on metric collection results. Docker and Node.js endpoints also require their own periodic collectors writing to `DockerMetric` and `NodeServerMetric` tables for historical charts.
- **Depends on**: Nothing — auth middleware exists, real-time queries are independent
- **Scope**: Implement all monitoring GET endpoints from `api_specification.md`. Add Docker and Node.js periodic collectors alongside the existing system metric collector.
- **Suggested subtasks**:
  - Implement `GET /api/processes` (list running processes via `/proc` directory scan — PID, name, user, CPU %, memory MB, uptime)
  - Implement `GET /api/services` (list systemd services via `systemctl list-units --type=service --output=json`)
  - Implement `GET /api/docker/containers` (list containers via Docker CLI or Docker Engine API)
  - Implement `GET /api/docker/storage` (Docker disk usage via `docker system df --format json`)
  - Implement Docker metric collector (periodic container CPU/memory stats → DockerMetric table)
  - Implement `GET /api/docker/containers/:id/metrics` (historical container metrics from DB, same period params as dashboard)
  - Implement `GET /api/nodejs/servers` (find Node.js processes in `/proc`, resolve port from `/proc/:pid/net/tcp`)
  - Implement Node.js server metric collector (periodic CPU/memory for Node.js PIDs → NodeServerMetric table)
  - Implement `GET /api/nodejs/servers/:pid/metrics` (historical Node.js metrics from DB)

### 3. Logs & Notifications API

- **Priority**: Medium
- **Rationale**: Completes the backend API surface. Logs and Notifications pages are built and waiting. Notification generation logic creates the events visible in the header bell badge. Can be worked in parallel with Task 2.
- **Depends on**: Nothing — auth middleware exists
- **Scope**: Implement log and notification endpoints per `api_specification.md`. System logs from journalctl. Application logs from log files. Notifications persisted in DB with generation logic for key events.
- **Suggested subtasks**:
  - Implement `GET /api/logs/system` (query journalctl with level filter and limit — parse JSON output)
  - Implement `GET /api/logs/application` (read from application log files with source filter)
  - Implement `GET /api/notifications` (paginated list from DB with read/unread filter)
  - Implement `PATCH /api/notifications/:id/read` (mark single notification as read)
  - Implement notification generation logic (watch for: service stopped events, disk mount issues, user login events — create Notification records)

### 4. Users Management API (Admin Only)

- **Priority**: Medium
- **Rationale**: Admin endpoints for user management and access code generation. The Users page is built. Access code generation is also needed to complete the signup flow end-to-end (the signup endpoint exists but code validation may be a stub pending the code generator).
- **Depends on**: Nothing — auth + requireAdmin middleware exist
- **Scope**: Implement user management and access code endpoints per `api_specification.md`, all behind admin middleware. Confirm access code validation is wired in signup.
- **Suggested subtasks**:
  - Implement `GET /api/users` (list all users — admin only)
  - Implement `POST /api/users` (admin creates user directly — admin only)
  - Implement `DELETE /api/users/:id` (admin only, with self-delete guard)
  - Implement `POST /api/users/access-code` (generate time-limited code tied to email — admin only)
  - Verify/complete access code validation in `POST /api/auth/signup` (code exists, matches email, not expired, not already used — mark as used on success)

### 5. Frontend API Integration

- **Priority**: Medium
- **Rationale**: All frontend pages use static/mock data. This task wires every page to the real backend APIs and WebSocket. Can begin incrementally — auth integration (login/signup) can start immediately, and each page can be connected as its API becomes available.
- **Depends on**: Tasks 1–4 (but can begin incrementally in parallel as each API is built)
- **Scope**: Replace all mock data with real API calls. Set up API client, auth state management, WebSocket client, and data fetching for every page.
- **Suggested subtasks**:
  - Set up API client (fetch wrapper or axios with cookie auth, base URL config, error handling)
  - Implement auth context/provider (login, logout, session check on load, current user state)
  - Add protected route guard (redirect to `/login` if unauthenticated, check role for admin routes)
  - Connect Login and Signup pages to auth API (`/api/auth/login`, `/api/auth/signup`)
  - Set up WebSocket client (connect on auth, handle reconnection, receive metric broadcasts)
  - Connect Dashboard to REST APIs (summary, historical metrics per chart, storage mounts) + WebSocket live updates
  - Connect Processes page to `GET /api/processes`
  - Connect Services page to `GET /api/services`
  - Connect Docker page to containers, container metrics, and storage endpoints
  - Connect Node.js page to servers and server metrics endpoints
  - Connect Logs page to system and application log endpoints
  - Connect Notifications page + header bell badge to notifications endpoints
  - Connect Users page (admin) to user list, create, delete, and access code endpoints
  - Connect Profile page to user update / password change endpoint

---

## Suggested Subtasks

> All existing tasks are `Done`. No additional subtasks for existing tasks.

_None — all existing tasks are complete._

---

## Summary

All foundation work is complete. The data pipeline (metric collection) finished today. The critical path is now **backend API development** starting with the Dashboard, followed by frontend wiring.

Recommended order of work:

1. **Dashboard API + WebSocket** — Highest impact; makes the primary view functional end-to-end with real data
2. **Monitoring API Endpoints** — Powers 4 pages; Processes and Services are pure real-time queries (quick wins); Docker/Node.js need their own collectors
3. **Logs & Notifications API + Users Management API** — Can be parallelized; complete the full API surface
4. **Frontend API Integration** — Start auth pages immediately; connect remaining pages incrementally as each API ships

> Tasks 1 and 2 are the critical path. Tasks 3 and 4 can be parallelized with each other and partially with Task 2. Task 5 (Frontend) can begin auth integration now and layer in page connections as each backend API completes.
