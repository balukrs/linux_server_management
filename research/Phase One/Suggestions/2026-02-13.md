---
date: 2026-02-13
generated_by: claude
---

# Next Task Suggestions — 2026-02-13

## Follow-up from Previous Suggestions

> Status of items suggested in previous runs.

| Date       | Suggestion                             | Status                               |
| ---------- | -------------------------------------- | ------------------------------------ |
| 2026-02-09 | Resolve Pending Architecture Decisions | Actioned — Task completed 2026-02-09 |
| 2026-02-09 | Database Setup & Schema                | Actioned — Task completed 2026-02-13 |
| 2026-02-09 | Backend Authentication & Authorization | Pending                              |
| 2026-02-09 | Shared Types Package                   | Pending                              |
| 2026-02-09 | Frontend Auth Integration & Routing    | Pending                              |
| 2026-02-09 | System Metric Collection Service       | Pending                              |
| 2026-02-09 | Dashboard API Endpoints                | Pending                              |
| 2026-02-09 | Monitoring API Endpoints               | Pending                              |
| 2026-02-09 | Logs & Notifications API Endpoints     | Pending                              |
| 2026-02-09 | Users Management API                   | Pending                              |

> 2 of 10 suggestions actioned. The remaining 8 are all backend/integration work that was blocked by database setup — now unblocked.

---

## Current Project Snapshot

- **Tasks Done**: 4
- **Tasks In Progress**: 0
- **Tasks To Do**: 0
- **Subtasks Done**: 11
- **Subtasks In Progress**: 0
- **Subtasks To Do**: 0
- **Open Blockers**: 0
- **Pending Decisions**: 0

> All frontend UI, architecture decisions, and database setup are complete. The project has **zero active tasks** — new tasks must be created to continue.

---

## Resolve First

> No blocking decisions or open blockers. Backend development is fully unblocked.

_Nothing to resolve — proceed with task creation._

---

## Suggested Tasks

> New main tasks recommended for creation, ordered by dependency.

### 1. Backend Authentication & Authorization

- **Priority**: High
- **Rationale**: Every protected API endpoint depends on auth middleware. Frontend auth pages (Login, Signup) are built and waiting for backend integration. Database and User model are ready.
- **Depends on**: Nothing — database is set up, JWT decision is made
- **Scope**: Implement JWT auth with HTTP-only cookies per `api_specification.md` and Decision log. Includes login, logout, me endpoints, auth middleware, role-based access, and signup with access code validation.
- **Suggested subtasks**:
  - Implement `POST /api/auth/login` (validate credentials, hash comparison, issue JWT access + refresh tokens as HTTP-only cookies)
  - Implement `POST /api/auth/logout` (clear cookies)
  - Implement `GET /api/auth/me` (return current user from JWT)
  - Implement `POST /api/auth/signup` (validate access code, create user)
  - Create auth middleware (verify JWT from cookie, attach user to request, handle token refresh)
  - Create role-based authorization middleware (`requireAdmin`)

### 2. Define Shared Types Package

- **Priority**: High
- **Rationale**: `packages/shared` exists but has no types. Both frontend and backend need shared TypeScript interfaces for type-safe API integration. Can be done in parallel with Task 1.
- **Depends on**: Nothing — derives from `api_specification.md` and `db_schema.md`
- **Scope**: Define TypeScript types/interfaces in `packages/shared` for all API request/response shapes, entity types, enums, and error types.
- **Suggested subtasks**:
  - Define entity types (User, SystemMetric, DockerMetric, NodeServerMetric, Notification, SystemLog, ActivityLog, AccessCode)
  - Define API response types (DashboardSummary, DashboardMetrics, ProcessInfo, ServiceInfo, ContainerInfo, DockerStorage, NodeServerInfo, MountInfo)
  - Define enums and constants (Role, NotificationType, LogLevel, LogSource, MetricType, MetricPeriod)
  - Define API error response type and error codes

### 3. System Metric Collection Service

- **Priority**: High
- **Rationale**: Core Phase One feature. The backend must collect system metrics from the Raspberry Pi every 15 seconds and persist to PostgreSQL. Required before Dashboard or any monitoring API can return real data.
- **Depends on**: Task 1 (auth not strictly required, but should be in place for the API layer)
- **Scope**: Background service in `apps/api` that reads CPU, memory, disk, network metrics from Linux system interfaces (/proc, system commands) and stores in SystemMetric table at 15-second intervals.
- **Suggested subtasks**:
  - Create Linux system interface module (read /proc/stat, /proc/meminfo, /proc/net/dev, df, etc.)
  - Implement CPU metric collection (overall usage %, load average)
  - Implement memory metric collection (total, used, available, swap)
  - Implement disk metric collection (per-mount usage)
  - Implement network metric collection (per-interface bytes in/out)
  - Create metric scheduler (15-second interval using setInterval or node-cron)
  - Persist collected metrics to SystemMetric table with timestamps

### 4. Dashboard API & WebSocket

- **Priority**: High
- **Rationale**: Dashboard is the primary view users see. Needs REST endpoints for initial load and WebSocket for live 15-second updates (per Decision: WebSocket for real-time).
- **Depends on**: Tasks 1, 3 (Auth + Metric Collection)
- **Scope**: Implement `GET /api/dashboard/summary` and `GET /api/dashboard/metrics` per `api_specification.md`. Set up WebSocket server (ws or socket.io) to push live metrics to connected clients.
- **Suggested subtasks**:
  - Implement `GET /api/dashboard/summary` (current CPU, memory, disk, network, uptime)
  - Implement `GET /api/dashboard/metrics` (historical data with type and period query params)
  - Set up WebSocket server integrated with Express
  - Implement WebSocket authentication (validate JWT on connection handshake)
  - Broadcast new metrics to connected clients every 15 seconds

### 5. Monitoring API Endpoints (Processes, Services, Docker, Node.js, Storage)

- **Priority**: Medium
- **Rationale**: These serve the remaining monitoring pages. All are read-only endpoints that query the Linux system in real-time.
- **Depends on**: Tasks 1 (Auth middleware)
- **Scope**: Implement all monitoring GET endpoints from `api_specification.md`
- **Suggested subtasks**:
  - Implement `GET /api/processes` (list running processes via ps/proc with CPU, memory, uptime)
  - Implement `GET /api/services` (list systemd services via systemctl)
  - Implement `GET /api/docker/containers` (list containers via Docker CLI/API)
  - Implement `GET /api/docker/containers/:id/metrics` (historical container metrics from DB)
  - Implement `GET /api/docker/storage` (Docker disk usage breakdown)
  - Implement `GET /api/nodejs/servers` (find Node.js processes, extract port/PID/metrics)
  - Implement `GET /api/nodejs/servers/:pid/metrics` (historical Node.js server metrics from DB)
  - Implement `GET /api/storage/mounts` (mount point info via df/mount)
  - Implement Docker metric collection (periodic container CPU/memory stats, persist to DockerMetric table)
  - Implement Node.js server metric collection (periodic stats, persist to NodeServerMetric table)

### 6. Logs & Notifications API

- **Priority**: Medium
- **Rationale**: Completes the monitoring feature set. Notification UI already exists in the frontend.
- **Depends on**: Tasks 1 (Auth middleware)
- **Scope**: Implement log and notification endpoints per `api_specification.md`. Logs read from journalctl/syslog. Notifications persisted in DB.
- **Suggested subtasks**:
  - Implement `GET /api/logs/system` (read from journalctl with level filter)
  - Implement `GET /api/logs/application` (read from application log files with source filter)
  - Implement `GET /api/notifications` (from DB, with read/unread filter)
  - Implement `PATCH /api/notifications/:id/read` (mark as read)
  - Implement notification generation logic (detect service stops, disk mount issues, user logins → create Notification records)

### 7. Users Management API (Admin Only)

- **Priority**: Medium
- **Rationale**: Admin endpoints for managing users and generating access codes. Users page UI is built.
- **Depends on**: Tasks 1 (Auth + requireAdmin middleware)
- **Scope**: Implement user management and access code endpoints per `api_specification.md`, all behind admin middleware.
- **Suggested subtasks**:
  - Implement `GET /api/users` (list all users, admin only)
  - Implement `POST /api/users` (create user, admin only)
  - Implement `DELETE /api/users/:id` (delete user, admin only, with self-delete protection)
  - Implement `POST /api/users/access-code` (generate access code for email, admin only)

### 8. Frontend API Integration

- **Priority**: Medium
- **Rationale**: All frontend pages use mock data. Once backend APIs exist, the frontend must be wired to real data. This is the final integration step.
- **Depends on**: Tasks 1-7 (Backend APIs must exist)
- **Scope**: Replace mock data with real API calls. Set up API client, auth state, WebSocket connection, and data fetching for all pages.
- **Suggested subtasks**:
  - Set up API client (axios or fetch wrapper with cookie auth, base URL config)
  - Implement auth context/provider (login, logout, session check on load, user state)
  - Add protected route guard (redirect to /login if unauthenticated)
  - Connect Dashboard to WebSocket + REST APIs (live metrics, historical charts, storage mounts)
  - Connect Processes, Services, Docker, Node.js pages to monitoring APIs
  - Connect Logs page to logs API (system + application tabs)
  - Connect Notifications page + header bell to notifications API
  - Connect Users page (admin) to users + access code APIs
  - Connect Profile page to user update endpoint

---

## Suggested Subtasks

> All existing tasks are `Done`. No additional subtasks for existing tasks.

_None — all existing tasks are complete._

---

## Summary

The project is at the **backend development phase**. Frontend UI, architecture decisions, and database are all complete. The critical path is:

Recommended order of work:

1. **Backend Authentication** — Gateway to every protected endpoint; unblocks all API work
2. **Define Shared Types** — Can be done in parallel with #1; enables type-safe frontend-backend integration
3. **System Metric Collection Service** — Core Phase One feature; the data pipeline that feeds the dashboard
4. **Dashboard API + WebSocket** — Primary user-facing feature; depends on metric collection
5. **Monitoring API Endpoints** — Processes, Services, Docker, Node.js, Storage (can parallelize once auth is ready)
6. **Logs & Notifications API** — Can parallelize with #5
7. **Users Management API** — Can parallelize with #5-6
8. **Frontend API Integration** — Final step: wire all pages to real backend data

> Tasks 1-2 can be worked in parallel. Tasks 5-7 can be parallelized once auth middleware exists. Task 8 can begin incrementally as each backend API becomes available.
